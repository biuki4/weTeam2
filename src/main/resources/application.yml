server:
  port: 8080
  servlet:
    context-path: /
  tomcat:
    uri-encoding: UTF-8

spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  datasource:
    url: jdbc:mysql://39.105.190.51:3306/weteam?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      filter:
        wall:
          config:
            condition-op-xor-allow:

  jpa:
    database: MYSQL
#    show-sql: true
    hibernate:
      ddl-auto: update
  http:
    encoding:
      charset: utf-8
      force: true
      enabled: true

  redis:
    host: 39.105.190.51     # 服务器地址
    port: 6379          # 端口
    timeout: 6000       # 链接超时时间 ms
    jedis:
      pool:             # 连接池
        max-active: 8   # 最大激活数
        max-idle: 8     # 最大空闲数
        max-wait: -1    # 最大等待时间
        min-idle: 0     # 最小空闲活动连接数
    password: 123456

  # 文件上传
  servlet:
    multipart:
      max-file-size: 5MB      # 单文件
      max-request-size: 10MB  # 总文件


#mybatis:
#  type-aliases-package: com.weteam.model
#  mapper-locations: classpath*:mapper/*.xml

#自定义属性
wechat:
  appId: wx40c243ab429ab596
  appSecret: 270ae7b609453ea5af9510a9271df25d
  url: https://api.weixin.qq.com/sns/jscode2session

upload:
  avatarPath: https://www.iamk.top/weTeam-file/avatar/
